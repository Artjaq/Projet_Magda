# Configuration Netlify — Tout au Chaud
# Authentification gérée par DecapBridge (PKCE) — aucune configuration Netlify Identity requise.

[build]
  command   = "npm run build"
  publish   = "_site"

[build.environment]
  NODE_VERSION = "20"
